[project]
name = "python-tools"
version = "1"
description = "Just to satisfy the dependencies of the Python tools in this repository."
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.12.5",
    "pydantic-ai>=1.51.0",
    "termcolor>=3.2.0",
    "xxhash>=3.6.0",
]
